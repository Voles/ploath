window.app.controller('ControlsController', ['$scope', ($scope) ->

	$scope.playPause = () ->
		console.log 'Play / pause'
		return
		
	$scope.next = () ->
		console.log 'Next'
		return

	$scope.previous = () ->
		console.log 'Previous'
		return

])